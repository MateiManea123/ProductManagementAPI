// Minimal Program.cs - entry point
using System;

namespace ProductModule
{
    internal static class Program
    {
        private static void Main(string[] args)
        {
            Console.WriteLine("ProductModule loaded. Types have been split into separate files under Models/, DTOs/, Requests/, Mapping/, Mapping/Resolvers/, Contracts/, Logging/, Handlers/.");
        }
    }
}
